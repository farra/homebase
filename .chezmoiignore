# Files that should NOT be copied to ~/ during chezmoi apply
# Only dot_* and private_dot_* files are actual dotfile targets

README.md
BOOTSTRAP.md
CLAUDE.md
DECISIONS.md
LICENSE

# Build and config files
Brewfile
homebase.toml
secretspec.toml
justfile
flake.nix
flake.lock

# Backlog and design docs
README.org
CHANGELOG.md
backlog.org
org-setup.org

# Directories
images
bootstrap
scripts
docs
.github

# Claude Code plugin state — machine-specific install paths + churning
# lastUpdated timestamps; regenerated per-machine. The enabledPlugins block in
# dot_claude/settings.json is the real declaration, so these never need syncing.
.claude/plugins/installed_plugins.json
.claude/plugins/known_marketplaces.json
