# Homebase runtime secrets — resolved by 1Password CLI
# Managed by chezmoi — edit source in homebase repo

# GitHub (required — used by gh CLI, GitHub API, container pulls)
GITHUB_TOKEN=op://Private/{{ .op_github_pat }}/credential
GH_TOKEN=op://Private/{{ .op_github_pat }}/credential

# AI API keys (optional — skip items left empty during chezmoi init)
{{- if ne .op_anthropic_key "" }}
ANTHROPIC_API_KEY=op://Private/{{ .op_anthropic_key }}/credential
{{- end }}
{{- if ne .op_openai_key "" }}
OPENAI_API_KEY=op://Private/{{ .op_openai_key }}/credential
{{- end }}
