# SSH Configuration
# Note: SSH keys are installed from 1Password via run_once script (first apply only)

Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519
    IdentitiesOnly yes
    AddKeysToAgent yes
